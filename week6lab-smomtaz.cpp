#include <iostream>

using namespace std;

int main()
{

int a = 50;
a = grade;

{
cout<<"Enter score as number out of 100";
if (a >= 86)
cout<<"grade = A"<< endl;
}

    {
    cout<<"Enter score as number out of 100";
    if (a >= 72)
    cout<<"grade = B"<< endl;
    }

        {
        cout<<"Enter score as number out of 100";
        if (a >= 60)
        cout<<"grade = C"<< endl;
        }

            {
            cout<<"Enter score as number out of 100";
            if (a >= 50)
            cout<<"grade = D"<< endl;
            }

                {
                cout<<"Enter score as number out of 100";
                if (a < 50)
                cout<<"grade = F"<< endl;
                }
           

return 0;
}